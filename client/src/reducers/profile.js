const initialState = {
  profile: null,
  profiles: [],
  repos: [],
  loading: true,
  error: {},
};

export default function (state = initialState) {
  const { type, payload } = action;

  switch (type) {
  }
}
